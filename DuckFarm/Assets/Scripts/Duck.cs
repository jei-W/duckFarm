using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Duck : ObjectBase
{
    enum GrowthLevel { egg, duckling, adult }
    enum WorkState { idle, sleep, work }
    bool male;
    int age = 0;

    //void 건물에 들어갈 수 있는지 물어본다
}
